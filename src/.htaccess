# Fully deny direct web access to the src/ directory.
# Classes are loaded by the autoloader from the filesystem, not needed over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
